package com.s333329.mappe2;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.util.Log;

public class MinBroadcastReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
/*
        if ("com.example.app.ACTION_SMS_SERVICE_ON".equals(intent.getAction())) {
            Intent serviceIntent = new Intent(context, MinPeriodisk.class);
            context.startService(serviceIntent);

            Log.d("MinBroadcastReceiver", "Broadcast received, starting service");
        }



 */
    }

}

